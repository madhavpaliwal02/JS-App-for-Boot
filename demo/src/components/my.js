import React from "react";

function Header({name, title}){
    return(
        <div style={ {background:""} }>
            <h1>This is my Header App.</h1>
        </div>
    )
}

export default Header;